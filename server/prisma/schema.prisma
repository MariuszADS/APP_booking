generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User{
  id Int @unique
  email String @unique
  name String
}

model Booking{
  id Int @unique
  booked_date Int
  appoitment_date Int
  comment String?     
}

model Service{
  id Int @unique
  type_service String
  comment String?
}